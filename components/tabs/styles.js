import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tabsWrapper: {
    flexDirection: "row",
  },
  tab: {
    padding: 10,
    borderBottomColor: "blue",
  },
  title: {
    fontWeight: 600,
  },
});
